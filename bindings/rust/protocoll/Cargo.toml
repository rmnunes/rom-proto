[package]
name = "rom"
version = "0.1.0"
edition = "2021"
description = "Safe Rust bindings for the rom state streaming library"
license = "MIT"

[dependencies]
rom-sys = { path = "../protocoll-sys" }

[dev-dependencies]
